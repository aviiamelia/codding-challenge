import { HeaderContainer, CardRoutes } from "./styles";

const Header = () => {
  return (
    <HeaderContainer>
      <CardRoutes>Home</CardRoutes>
    </HeaderContainer>
  );
};

export default Header;
